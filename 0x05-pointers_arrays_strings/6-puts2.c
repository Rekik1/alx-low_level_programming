#include "main.h"
#include <stdio.h>

/**
 * puts2 - prints every other character of a string
 * @str: string
 */

void puts2(char *str)
{
int i;
int a = 0;

while (str[a] !=  '\0')
{
a++;
}
t = longi - 1;
for (i = 0 ; i < t ; i += 2)
{
_putchar(str[i]);
}
_putchar('\n');
}
